# Прогресс разработки

Последнее обновление: 3 сентября 2026 года.

## Сейчас

Этап 0 — завершён.

## Обнаружено

- Git CLI установлен, но текущая папка ещё не инициализирована как Git-репозиторий.
- Установлен .NET SDK 10.0.302; консольный проект нацелен на .NET 10.
- До начала этапа уже существовал `NutriFlow.Console` с ранним учебным черновиком классов и расчёта. Этот код сохранён без переработки.

## Сделано

- создан solution `NutriFlow.sln`;
- существующий консольный проект добавлен в solution;
- добавлено видимое сообщение при запуске;
- добавлены проектные документы и `.gitignore`.

## Проверка

- `dotnet build .\NutriFlow.sln` — успешно, 0 ошибок и 2 предупреждения nullable-анализа в существовавшем черновике `Product`.
- `dotnet run --project .\NutriFlow.Console\NutriFlow.Console.csproj --no-build` — успешно, выведено `NutriFlow запущен.`.
- Автоматические тесты не запускались: на этапе 0 тестового проекта ещё нет; он запланирован на этапе 3.

Предупреждения относятся к свойствам `Product.Name` и `Product.Nutrition`, которые могут остаться без начального значения. Исправление войдёт в осмысленную переработку модели одного продукта на этапе 1.

## Следующий шаг

После команды «продолжай» — этап 1: понятный консольный расчёт КБЖУ одного продукта.
